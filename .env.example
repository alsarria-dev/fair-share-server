MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/fair-share
JWT_SECRET=replace-with-a-long-random-secret
PORT=5005

# Optional — overrides the default CORS origin (https://app-fair-share.vercel.app)
# for local frontend development, e.g.:
# CORS_ORIGIN=http://localhost:3000
